type Order = {
    entity: any,
    entityId: string,
    orderId: string,
    paymentType: string,
    paymentService: string,
    amount: string | number,
    currency: string | number
}

export type {
    Order
}